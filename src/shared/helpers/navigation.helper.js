/**
 * Navigation Helper Functions
 * 
 * Utility functions for navigation-related operations
 * 
 * Helper functions to create:
 * - isActiveLink(pathname, href) - Check if link is active
 * - getNavigationByRole(role) - Get navigation items based on user role
 * - buildBreadcrumbs(pathname) - Generate breadcrumb navigation
 * - filterNavigationByPermission(items, permissions) - Filter nav items by permissions
 */

// Write your navigation helper functions here

